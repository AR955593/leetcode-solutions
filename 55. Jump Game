class Solution:
    def canJump(self, nums: List[int]) -> bool:
        maxs = 0
        for i in range(0,len(nums)):
            if i > maxs:
                return False
            maxs = max(maxs, i+nums[i])
        return True         
